require "rails_helper"

RSpec.describe BookPoro, type: :poro do 
	describe "initialize" do 
		it "exists" do 
			# assign book data to :data, :id, :type, :attributes, :books keys

			# expect new Book Poro instantiation to create a book with same data structure provided in reqs
		end
	end
end 